# Progressing-character-strings-in-c
PROCESSING OF CHARACTER STRINGS. WORKING WITH FILES

The program which is in( sy_mbols.c ) program file, creates and displays a text file (final.txt) containing only
sequences of characters which is in the "text.txt" file contained within brackets “(” –
")". Translate the symbols in these lines into their numerical representation and
calculate their sum.